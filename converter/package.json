{
  "name": "tokens-sync",
  "version": "0.2.0",
  "private": true,
  "description": "Raw Figma variables JSON → DTCG token tree. Native TS, no build step.",
  "type": "module",
  "bin": {
    "tokens-sync": "./cli.ts"
  },
  "scripts": {
    "convert": "node ./cli.ts",
    "test": "node --test *.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
